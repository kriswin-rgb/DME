plugin: aws_ec2
regions:
  - us-east-1

filters:
  tag:Environment: production

keyed_groups:
  - key: tags.Environment
    prefix: tag

compose:
  ansible_host: public_ip_address

strict: False
profile: ECR_image
